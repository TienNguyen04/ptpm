# Server Configuration
server:
  port: 8081
servlet:
 context-path: /user-service
# Database Configuration
spring:
  datasource:
    url: jdbc:postgresql://26.41.201.183:5050/bankdb
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: user_service
        jdbc:
          time_zone: Asia/Ho_Chi_Minh

# Application Configuration
app:
  jwt:
    secret: /dDMtv80KZSwPmYUT6r0Qj6LJNpSwCmwt7hqv0x5hc+RgNz3v6bFYVF3IrWjTsxLj//ddVXi34mNrEZ454nmGg==
    expiration: 86400000 # 24 hours in milliseconds
# Logging
logging:
  level:
    com.bank.userservice: DEBUG
    org.springframework.security: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, info, metrics
  endpoint:
    prometheus:
      enabled: true